
针对上述问题，本⽂提出 Verifier-Guided Personalized Context Compression 框架，核⼼⽬标是
在有限 token 预算下压缩⽤⼾⻓期上下⽂，同时尽可能保留当前请求所需的下游个性化效⽤。具体⽽
⾔，本⽂⾸先提出 Personalized Context Sufficiency Verification（PCSV） 任务：给定当前⽤⼾
请求、完整⽤⼾上下⽂以及压缩后的⽤⼾上下⽂，判断压缩上下⽂是否仍然⾜以⽀持个性化⽣成或个
性化偏好判断。与传统上下⽂压缩评估不同，PCSV 不仅要求 verifier 判断压缩上下⽂是否
sufficient，还希望其进⼀步识别缺失的个性化证据类型，例如稳定偏好、近期偏好更新、⻛格证据、
任务相关记忆、硬性约束、偏好冲突或错误⽤⼾上下⽂。基于这⼀验证结果，本⽂进⼀步将 verifier 作
为上下⽂压缩优化的反馈信号，形成“压缩―验证―修复―再验证”的闭环，使系统不再盲⽬依赖固
定压缩率或⼀次性摘要，⽽是能够根据当前请求和压缩结果动态调整上下⽂预算，并有针对性地补充
缺失信息。

实现这⼀⽬标⾯临两个主要挑战。⾸先，在线 verifier 不能直接读取完整⽤⼾上下⽂。否则，verifier
本⾝就会消耗⼤量 token，削弱上下⽂压缩带来的效率收益。因此，系统需要在不访问完整历史的情
况下，利⽤低成本替代信号判断压缩上下⽂是否存在个性化信息不⾜⻛险。其次，个性化充分性难以
监督。压缩上下⽂是否⾜够并不等同于压缩摘要是否语义相似，⽽取决于其是否保留了当前任务所需
的个性化证据。不同请求所需的证据类型不同，同⼀⽤⼾的偏好也可能随场景和时间变化。因此，需
要构造能够反映下游个性化效⽤的监督信号，并将其转化为可⽤于压缩优化的反馈。
为解决第⼀个挑战，本⽂设计了⼀种⾯向在线场景的个性化充分性 verifier。该 verifier 在线阶段不直
接读取完整⽤⼾历史，⽽是基于压缩上下⽂、轻量化⽤⼾记忆索引、压缩轨迹以及当前请求所需的个
性化证据类型进⾏判断。⽤⼾记忆索引⽤于记录每条记忆的类型、时间、重要性、隐私等级和短标
签；压缩轨迹则记录压缩器选择了哪些记忆、丢弃了哪些⾼重要性记忆、各类个性化证据的覆盖情况
以及压缩率等信息。通过这些低成本信号，verifier 不需要看到完整上下⽂中的所有原始⽂本，也能够
估计当前压缩上下⽂是否覆盖了完成任务所需的关键⽤⼾信息。当 verifier 判断上下⽂充分时，系统可
以直接进⾏⽣成；当其判断上下⽂不⾜时，系统则根据 verifier 输出的缺失证据类型触发 targeted
repair。

为解决第⼆个挑战，本⽂提出基于 verifier 的上下⽂压缩优化策略。在离线训练阶段，teacher
verifier 可以访问完整⽤⼾上下⽂，并⽐较完整上下⽂与压缩上下⽂下的下游输出差异，从⽽构造
sufficiency、utility loss、missing evidence type 和 repair action 等监督信号。系统可以分别使⽤完
整上下⽂和压缩上下⽂⽣成回答，并通过任务指标、个性化评价器或 LLM-as-a-judge 估计两者在个性
化效⽤上的差异。除此之外，本⽂还可以通过合成证据破坏构造诊断样本，以训练 verifier 识别不同类
型的个性化信息缺失。在此基础上，verifier 的 sufficiency score、预测的个性化效⽤损失和 missingevidence diagnosis 可以作为压缩器的优化信号，⽤于动态调整压缩预算、选择需要补充的记忆类
型，或训练更符合个性化效⽤⽬标的上下⽂压缩器。